{
  "name": "@sim/gas-analyzer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The simulated R 144 reference continuous gas monitor (CO + NOx) — sim-instruments instrument #2",
  "bin": {
    "sim-gas-analyzer": "./src/bin.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "tsx src/bin.ts"
  },
  "dependencies": {
    "@primmel/sst-runtime": "*",
    "@primmel/sst-runtime": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "vitest": "^4.1.10"
  }
}
