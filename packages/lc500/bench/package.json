{
  "name": "@sim/bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The standalone virtual-bench SPA (terminal + bench + how-it-works) — served by the sim, embedded by SMART",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sim/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
